import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pillbox.app',
  appName: 'Pillbox',
  webDir: 'dist/pillbox-app/browser'
};

export default config;
